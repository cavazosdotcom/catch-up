<div class="card">
  <h2 class="card-header" id="media-title">{{title}}</h2>
  <div class="card-body">
    <p id="media-type">{{type}}</p>
    <div class="mt-3">
        <p id="media-info">{{info}}</p>
    </div>
  </div>
</div>